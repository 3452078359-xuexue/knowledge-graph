# 贵阳市小红书数据—去除 Emoji 派生层

生成日期：`2026-09-16`

本目录是从 `09_贵阳市小红书数据` 生成的可用清洗层，用于后续语义审核和图谱挂载准备。原始来源文件保留，未被本次清洗覆盖。

## 清洗范围

- 删除 Unicode Emoji/图形符号，例如 `📍`、`✨`、`☀️`、`🌟`。
- 删除小红书平台表情标记，例如 `[心心眼R]`、`[笑哭R]`、`[doge]`。
- 删除对象占位符、损坏替代字符和私有区字符。
- `1️⃣` 等数字键帽转换为普通数字，避免丢失列表编号。

## 保留内容

- 普通中文、英文、数字、标点、URL 和业务 ID。
- `[话题]`、`[地点]` 等结构标记。
- 原始目录层级、文件名、记录顺序和非目标字段。

## 验收结果

- 7 个 CSV，共 6,791 条记录。
- 5,110 条记录、8,671 个单元格发生清洗变化。
- 删除 50,085 个非键帽 Emoji 序列、7,461 个平台表情标记、475 个异常特殊字符。
- 5,084 个数字键帽转为普通数字。
- 目标字段 Emoji、平台表情和异常特殊字符残留均为 0。
- 606/606 条非空证据片段仍是清洗后帖子正文的连续原文子串。
- 帖子正文长度和 SHA-256 已按清洗后内容重算。

## 使用注意

1. 本目录仍是“待审核派生数据”，未写入当前知识图谱。
2. POI 候选表不是正式实体，须完成实体消歧和证据审核后才能挂图。
3. 当前正式 POI 工作副本为 11 列；其 `外文/地图名` 列在本次清洗前已被外部编辑删除。本次保留该编辑，未擅自恢复或覆盖；12 列原版仍保留在原始来源目录。

详细统计见 `去除Emoji清洗报告.json`，独立验收见 `去除Emoji独立验证报告.json`。
